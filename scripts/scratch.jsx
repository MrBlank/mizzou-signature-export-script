#target illustrator var docRef          = app.activeDocument; // Select Textapp.executeMenuCommand ('Text Objects menu item');// Convert to outlineapp.executeMenuCommand ('outline');colorText();function colorText() {         var totalCompoundPaths = docRef.compoundPathItems.length;              //alert(totalCompoundPaths);                 for  (var i = 0; i < totalCompoundPaths; i++ ) {                      docRef.compoundPathItems[i].selected = true;                           var totalPaths = docRef.compoundPathItems[i].pathItems.length;                                         for (var j = 0; j < totalPaths; j++ ) {                          var newGreyColor = new GrayColor();                    newGreyColor.gray = 0;                                        docRef.compoundPathItems[i].pathItems[j].fillColor = newGreyColor;                                    }        } }   alert("Done! ");